• Terminal grid rendering: native pane width with pinch zoom and two-axis panning; tables align perfectly
• Seamless history: scroll up to load older output automatically — no buttons; scroll back down to resume live
• CJK monospace alignment: fullwidth characters take exactly two columns
• URLs and file paths tinted blue; mode banner colors match the Mac
• New mode-cycle button sends Shift+Tab to switch Claude's working mode
• Much smoother scrolling
